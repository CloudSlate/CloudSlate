{
  "name": "cloudslate-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for CloudSlate blog",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

